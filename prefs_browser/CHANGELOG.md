## 0.2.1

- Add `package:tekartik_prefs_browser/prefs_light.dart`:
  `getPrefsLightBrowser({String? name})` and `getPrefsLightBrowserOrNull`, a
  `PrefsLight` (so a `KvStore`) on top of local storage (`'<name>/<key>'`
  json encoded entries, name defaults to `prefs`).

## 1.0.0

- Initial version, created by Stagehand
